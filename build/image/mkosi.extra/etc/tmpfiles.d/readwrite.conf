# Runtime directories on tmpfs (/run).
# Everything else is either:
#   - baked into the read-only erofs root (mkosi.extra)
#   - on the ext4 data partition (/data, via data.mount)
#   - on volatile tmpfs (/var/log via var-log.mount, /var/tmp via var-tmp.mount)
d /run/sshd 0755 root root -
