[Content]
Packages=
    # Secure Boot chain: shim-signed (Microsoft-signed) -> grubx64.efi
    # (Canonical-signed). mkosi uses these pre-signed binaries directly,
    # skipping grub-mkimage entirely. This gives:
    #   UEFI Secure Boot -> shimx64.efi -> grubx64.efi -> UKI
    grub-efi-amd64-signed
    shim-signed

    # EFI stub for Unified Kernel Image generation
    systemd-boot-efi

Bootable=yes
Bootloader=grub
ShimBootloader=signed

KernelCommandLine=
    console=ttyS0,115200n8
    lockdown=integrity
    module.sig_enforce=1
    acpi_no_initrd_table_override
    root=PARTLABEL=root-x86-64
    rootfstype=ext4
    rw
